name: One Browser App

'on':
  workflow_dispatch:
    inputs:
      expected_action_sha:
        description: Dispatcher-resolved exact central Action commit SHA
        required: true
        type: string
      app_repository:
        description: One Browser App repository
        required: true
        type: string
      app_ref:
        description: Exact 40-character App commit SHA
        required: true
        type: string
      version:
        description: Optional requested version
        required: false
        type: string
      publish:
        description: Reserved; true fails closed because publication is not implemented
        required: false
        type: boolean
        default: false
      confirmation:
        description: Required only for a future mutation request
        required: false
        type: string

permissions:
  contents: read

jobs:
  policy:
    name: Reject mutable or privileged App requests
    runs-on: ubuntu-24.04
    permissions: {}
    steps:
      - name: Validate fixed non-publishing boundary
        shell: bash
        env:
          ACTION_REPOSITORY: ${{ github.repository }}
          ACTION_SHA: ${{ github.sha }}
          EXPECTED_ACTION_SHA: ${{ inputs.expected_action_sha }}
          APP_REPOSITORY: ${{ inputs.app_repository }}
          APP_REF: ${{ inputs.app_ref }}
          PUBLISH: ${{ inputs.publish }}
          CONFIRMATION: ${{ inputs.confirmation }}
        run: |
          set -Eeuo pipefail
          [ "$ACTION_REPOSITORY" = voiceofhu/one-action ] || {
            echo "App builds run only in the fixed central Action repository." >&2
            exit 1
          }
          [ "$APP_REPOSITORY" = voiceofhu/one-browser-app-next ] || {
            echo "App source must be the fixed One Browser App repository." >&2
            exit 1
          }
          [[ "$ACTION_SHA" =~ ^[0-9a-f]{40}$ ]] || {
            echo "Action revision is not an exact commit SHA." >&2
            exit 1
          }
          if [[ ! "$EXPECTED_ACTION_SHA" =~ ^[0-9a-f]{40}$ ]] \
            || [ "$EXPECTED_ACTION_SHA" != "$ACTION_SHA" ]; then
            echo "Dispatcher-bound Action SHA is missing, invalid, or stale." >&2
            exit 1
          fi
          [[ "$APP_REF" =~ ^[0-9a-f]{40}$ ]] || {
            echo "App ref must already be an exact 40-character commit SHA." >&2
            exit 1
          }
          [ "$PUBLISH" = false ] || {
            echo "App publication is not implemented; refusing before source resolution." >&2
            exit 1
          }
          [ -z "$CONFIRMATION" ] || {
            echo "Non-publishing App builds reject mutation confirmation input." >&2
            exit 1
          }

  prepare:
    needs: policy
    uses: ./.github/workflows/reusable-prepare.yml
    with:
      workflow_name: app
      expected_action_sha: ${{ inputs.expected_action_sha }}
      primary_label: app
      primary_repository: ${{ inputs.app_repository }}
      primary_ref: ${{ inputs.app_ref }}
      version: ${{ inputs.version }}
      publish: ${{ inputs.publish }}
      publish_supported: false
      confirmation: ${{ inputs.confirmation }}

  build:
    needs: prepare
    uses: ./.github/workflows/reusable-build-app.yml
    with:
      action_sha: ${{ github.sha }}
      app_repository: ${{ inputs.app_repository }}
      app_sha: ${{ needs.prepare.outputs.primary_sha }}
      version: ${{ inputs.version }}
