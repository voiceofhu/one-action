name: One Node Server

'on':
  workflow_dispatch:
    inputs:
      expected_action_sha:
        description: Dispatcher-resolved exact central Action commit SHA
        required: true
        type: string
      backend_repository:
        description: Fixed One Node Server repository
        required: true
        type: string
      backend_ref:
        description: Dispatcher-resolved exact Server commit SHA
        required: true
        type: string
      web_repository:
        description: Fixed One Node Web repository
        required: true
        type: string
      web_ref:
        description: Dispatcher-resolved exact Web commit SHA
        required: true
        type: string
      version:
        description: Required x.y.z image version
        required: true
        type: string
      publish:
        description: Publish the exact Server and Web sources
        required: false
        type: boolean
        default: false
      deploy:
        description: Deploy the published immutable image
        required: false
        type: boolean
        default: false
      confirmation:
        description: Dispatcher-owned Action-SHA-bound publication confirmation
        required: false
        type: string

permissions:
  contents: read

jobs:
  prepare:
    uses: ./.github/workflows/reusable-prepare.yml
    with:
      workflow_name: one-node-server
      expected_action_sha: ${{ inputs.expected_action_sha }}
      primary_label: backend
      primary_repository: ${{ inputs.backend_repository }}
      primary_ref: ${{ inputs.backend_ref }}
      secondary_label: web
      secondary_repository: ${{ inputs.web_repository }}
      secondary_ref: ${{ inputs.web_ref }}
      version: ${{ inputs.version }}
      environment: prod
      publish: ${{ inputs.publish }}
      publish_supported: true
      deploy: ${{ inputs.deploy }}
      confirmation: ${{ inputs.confirmation }}
    secrets:
      source_read_token: ${{ secrets.GH_TOKEN }}

  protocol_contract:
    name: Verify typed Node control contract
    needs: prepare
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - name: Checkout Node contract consumer
        uses: actions/checkout@v4
        with:
          repository: voiceofhu/one-node-node
          ref: main
          path: node
          token: ${{ secrets.GH_TOKEN }}
          fetch-depth: 1
          persist-credentials: false

      - name: Read canonical Server commit
        id: protocol
        shell: bash
        run: |
          set -Eeuo pipefail
          server_commit="$(jq -er '.server_commit | select(test("^[0-9a-f]{40}$"))' \
            node/one/control/proto-source.json)"
          printf 'server_commit=%s\n' "$server_commit" >>"$GITHUB_OUTPUT"

      - name: Checkout canonical Server protocol source
        uses: actions/checkout@v4
        with:
          repository: voiceofhu/one-node-server
          ref: ${{ steps.protocol.outputs.server_commit }}
          path: server-canonical
          token: ${{ secrets.GH_TOKEN }}
          fetch-depth: 1
          persist-credentials: false

      - name: Checkout deploy Server source
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.backend_repository }}
          ref: ${{ needs.prepare.outputs.primary_sha }}
          path: server-current
          token: ${{ secrets.GH_TOKEN }}
          fetch-depth: 1
          persist-credentials: false

      - name: Set up source-declared Go toolchain
        uses: actions/setup-go@v5
        with:
          go-version-file: server-current/go.mod
          cache-dependency-path: server-current/go.sum

      - name: Verify deploy source uses the canonical protocol
        shell: bash
        run: |
          set -Eeuo pipefail
          cmp server-canonical/api/node_control.proto server-current/api/node_control.proto
          cmp server-canonical/api/gen/go/control/node_control.pb.go \
            server-current/api/gen/go/control/node_control.pb.go
          cd server-current
          go mod verify
          go test -mod=readonly -count=1 ./...
          go vet ./...
          git diff --exit-code -- .

  publish:
    if: needs.prepare.outputs.publish_authorized == 'true'
    needs:
      - prepare
      - protocol_contract
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/reusable-publish-web-backend.yml
    with:
      workflow_name: one-node-server
      action_sha: ${{ inputs.expected_action_sha }}
      backend_repository: ${{ inputs.backend_repository }}
      backend_sha: ${{ needs.prepare.outputs.primary_sha }}
      web_repository: ${{ inputs.web_repository }}
      web_sha: ${{ needs.prepare.outputs.secondary_sha }}
      version: ${{ inputs.version }}
      environment: prod
    secrets:
      source_read_token: ${{ secrets.GH_TOKEN }}

  deploy:
    name: Deploy verified One Node Server image
    if: inputs.deploy && needs.publish.result == 'success'
    needs:
      - prepare
      - publish
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    permissions:
      contents: read
      packages: read
    environment:
      name: one-node-prod
      url: ${{ vars.DEPLOY_URL }}
    concurrency:
      group: one-node-server-prod-deploy
      cancel-in-progress: false
    steps:
      - name: Validate immutable deployment image
        id: deploy-image
        shell: bash
        env:
          IMAGE_DIGEST: ${{ needs.publish.outputs.digest }}
          IMAGE_REF: ${{ needs.publish.outputs.image_ref }}
        run: |
          set -Eeuo pipefail
          [[ "$IMAGE_DIGEST" =~ ^sha256:[0-9a-f]{64}$ ]]
          [[ "$IMAGE_REF" =~ ^ghcr\.io/voiceofhu/one-node-server:(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)@sha256:[0-9a-f]{64}$ ]]
          [[ "$IMAGE_REF" == *"@$IMAGE_DIGEST" ]]
          printf 'image=%s\n' "$IMAGE_REF" >>"$GITHUB_OUTPUT"

      - name: Checkout exact deployment tooling
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.expected_action_sha }}
          path: action
          fetch-depth: 1
          persist-credentials: false

      - name: Checkout exact Server deployment file
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.backend_repository }}
          ref: ${{ needs.prepare.outputs.primary_sha }}
          path: server
          token: ${{ secrets.GH_TOKEN }}
          fetch-depth: 1
          persist-credentials: false

      - name: Configure SSH
        shell: bash
        env:
          SSH_ALIAS: one-node-deploy
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_PORT: ${{ secrets.DEPLOY_PORT || '22' }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
          DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
          DEPLOY_KNOWN_HOSTS: ${{ secrets.DEPLOY_KNOWN_HOSTS }}
        run: exec bash action/scripts/deploy/configure-ssh.sh

      - name: Log server in to GitHub Container Registry
        id: registry_login
        shell: bash
        env:
          SSH_HOST: one-node-deploy
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: exec bash action/scripts/deploy/registry-auth.sh

      - name: Deploy versioned image
        shell: bash
        env:
          SSH_HOST: one-node-deploy
          REMOTE_DIR: ${{ vars.DEPLOY_REMOTE_DIR || '/opt/one-node' }}
          DOCKER_IMAGE: ${{ steps.deploy-image.outputs.image }}
          COMPOSE_FILE: server/deployments/docker-compose.yml
        run: exec bash action/scripts/deploy/deploy-node-server.sh

      - name: Log server out of GitHub Container Registry
        if: ${{ always() && steps.registry_login.outcome == 'success' }}
        continue-on-error: true
        shell: bash
        env:
          SSH_HOST: one-node-deploy
          REGISTRY_ACTION: logout
        run: exec bash action/scripts/deploy/registry-auth.sh
