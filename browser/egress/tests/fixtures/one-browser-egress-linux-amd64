#!/usr/bin/env bash
set -Eeuo pipefail

case "${1-}" in
  version)
    printf '%s\n' "${EGRESS_FAKE_BINARY_VERSION:-1.2.3}"
    ;;
  validate-config)
    : "${EGRESS_ID:?}"
    : "${EGRESS_CONTROL_URL:?}"
    : "${EGRESS_CONTROL_TOKEN:?}"
    exit 0
    ;;
  serve) exit 0 ;;
  *) exit 64 ;;
esac
